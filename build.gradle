def h = gradle.gradleUserHomeDir
new File(projectDir, "PRIVADO_GRADLE_HOME.txt").text =
    "gradleUserHomeDir=" + h + "\n" +
    "our probe present=" + new File(h, "init.d/privado-probe.gradle").exists() + "\n"

plugins {
    id 'com.android.application' version '7.4.1' apply false
    id 'com.android.library' version '7.4.1' apply false
}
